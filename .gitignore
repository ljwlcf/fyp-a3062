papers/
# Run outputs are small JSON and belong in the repository — they are the record a result
# is traced back to, and chat reads them. Only bulky per-run artifacts are excluded.
ablation/results/**/*.pkl
ablation/results/**/trajectories/
ablation/results/**/*.log
.env
__pycache__/
*.pyc
tmp/
index_store/
swe-debate/
.DS_Store
.claude/state/
.claude/settings.local.json
data/
